import React from 'react';

const Landing = () => {
    return (
    <div style={{textAlign : "center"}}>
        <h1>Emaily!</h1>
        <p>This site is made for testing purpose only, not for production.</p>
    </div>
    );
};

export default Landing;